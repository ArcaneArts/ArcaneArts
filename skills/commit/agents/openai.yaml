interface:
  display_name: "Commit"
  short_description: "Draft broad sync commits from active repo changes"
  default_prompt: "Use $commit to inspect the full working tree, factor in the active conversation, and create a broad sync commit unless a narrower scope is explicitly requested."
